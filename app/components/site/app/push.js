// The morning push, verbatim from the backend's hand-written set
// (notifications.py _AUTO_GENERIC). Plain module: both the lock screen (a
// client component) and the page (a server component) read it.
export const PUSH = { title: 'Quiet luck', body: 'It’s tilted your way today — come see how far ›' };
